package algorithms;

public class BinarySearch {

    /**
     * 二分查找基礎版
     * Params: a-待查找的升序陣列
     *         target-待查找的目標值
     * Returns:
     *         找到則返回索引
     *         找不到就返回 -1
     */
    public static int binarySearch(int[] arr, int target) {
        return -1;
    }
}
